# Chat-Application
This project is about creating a Chat Application using TCP and UDP in Python. It shows how real-time communication works using socket programming. TCP ensures reliable message delivery, while UDP provides fast, connectionless communication. The project demonstrates server–client interaction, message exchange, and protocol behavior.
